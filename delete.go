package riak

import (
	"net/http"
	"net/url"
)

// Delete removes the specified object from the database
func (c *Client) Delete(o *Object, opts map[string]string) error {
	// TODO: this whole function
	return nil
}
